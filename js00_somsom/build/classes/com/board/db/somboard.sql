DROP SEQUENCE SOMSEQ;
DROP TABLE SOMIBOARD;

CREATE SEQUENCE SOMSEQ;

--번호, 이름, 제목, 내용, 날짜
CREATE TABLE SOMIBOARD(
	SEQ NUMBER PRIMARY KEY,
	NAME VARCHAR2(1000) NOT NULL,
	TITLE VARCHAR2(2000) NOT NULL,
	CONTENT VARCHAR2(4000) NOT NULL,
	REGDATE DATE NOT NULL
);

INSERT INTO SOMIBOARD
VALUES(SOMSEQ.NEXTVAL, 'TESTNAME','TEST TITLE','TEST CONTENT',SYSDATE);

SELECT SEQ, NAME, TITLE, CONTENT, REGDATE
FROM SOMIBOARD;

